services:
  - type: web
    name: smart-qr-health
    env: docker
    plan: free
    region: singapore
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: PUBLIC_BASE_URL
        value: https://smart-qr-health.onrender.com